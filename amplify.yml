version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 22 && nvm use 22
        - npm ci
    build:
      commands:
        # - cp -a node_modules/govuk-frontend/dist/govuk/assets .amplify-hosting/static/assets
        - npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - "**/*"
